# Name of your application. Used to uniquely configure containers.
service: silmu

# Name of the container image (use your-user/app-name on external registries).
image: sdkdream50a-stack/silmu

# Deploy to these servers.
servers:
  web:
    - 141.164.53.97
  # job:
  #   hosts:
  #     - 141.164.53.97
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
#
proxy:
  ssl: true
  host: silmu.kr,www.silmu.kr,exam.silmu.kr
  healthcheck:
    path: /up
    interval: 3
    timeout: 60

deploy_timeout: 60

# Where you keep your container images.
registry:
  # GitHub Container Registry (no Docker Desktop required)
  server: ghcr.io
  username: sdkdream50a-stack

  # GitHub Personal Access Token with write:packages permission
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - ANTHROPIC_API_KEY
    - SILMU_DATABASE_PASSWORD
    - BLOG_API_KEY
  clear:
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true
    # 2 worker × 5 thread = 10 동시성. GVL 병목 분산 + CoW 메모리 절약.
    WEB_CONCURRENCY: 2
    RAILS_MAX_THREADS: 5
    GA_MEASUREMENT_ID: G-DGH1M308BH
    EXAM_GA_MEASUREMENT_ID: G-D3SL77M9GE
    ADSENSE_CLIENT_ID: ca-pub-6241798439911569

    # EXP-SILMU-NONADS-01 — exam.silmu.kr 수험 교재 CTA (CEO 승인 2026-09-12)
    # 무료 URL 은 조달청 발간자료 #117 «공공조달관리사 표준교재» 원문이다.
    # 2026-09-12 실측: HTTP 200 · 「공공조달관리사 표준교재(배포용)」 첨부 · 공공누리 제3유형.
    # 그 게시물 본문이 못박는다 — "표준교재는 해당 교재가 유일하며 타 교재는 조달청과 무관".
    # 그래서 CTA 는 무료 원문을 1순위로 두고 시판 교재를 «필요하면» 2순위로만 보여준다.
    EXAM_BOOK_CTA_ENABLED: "1"
    EXAM_BOOK_FREE_URL: "https://www.pps.go.kr/kor/bbs/view.do?bbsSn=2601150031&key=00034"
    # EXAM_BOOK_AFFILIATE_URL 은 **의도적으로 비어 있다.** 쿠팡파트너스에 exam.silmu.kr 이
    # 채널로 등록됐는지 확인하지 못했고(브라우저 접근이 확장 안전정책으로 차단 · Open API 에
    # 채널 엔드포인트 없음), 미등록 채널 게시는 정책 위반이다. 파티셜이 fail-closed 라
    # 이 값이 없으면 제휴 링크와 고지 문구가 통째로 렌더되지 않는다.
    # 값을 넣기 전 조건: ① 채널 등록 확인 ② 딥링크를 subId=sxexambook01 로 발급
    # ③ 발급 결과를 되읽어 subId 가 실제로 박혔는지 단언. 사후 URL 문자열 치환 금지.
    ADMIN_EMAIL: sdkdream50a@gmail.com
    DB_HOST: 172.18.0.1
    # CF 우회 차단: observe(로그만) → enforce(403 차단). off로 즉시 롤백.
    CF_ORIGIN_LOCKDOWN: enforce

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Match this to any external database server to configure Active Record correctly
    # Use silmu-db for a db accessory server on same machine via local kamal docker network.
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "silmu_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64
  # Build on remote server and push to GitHub Container Registry
  remote: ssh://root@141.164.53.97:2222
  # Pass secrets to Docker build process
  secrets:
    - RAILS_MASTER_KEY

ssh:
  port: 2222

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: valkey/valkey:8
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
