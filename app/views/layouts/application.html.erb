<!DOCTYPE html>
<html lang="ko">
  <head>
    <title><%= content_for(:title) || "실무 - 감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="실무">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="공무원을 위한 실무 가이드 - 감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원이 되세요.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link href="https://cdn.jsdelivr.net/gh/toss/tossface/dist/tossface.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
    <style>
      .material-symbols-outlined {
        font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
      }
    </style>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50 text-gray-900 antialiased">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center space-x-2" do %>
              <div class="w-8 h-8 bg-navy-600 rounded-lg flex items-center justify-center">
                <span class="text-white font-bold text-lg">실</span>
              </div>
              <span class="text-xl font-bold text-navy-600">실무</span>
            <% end %>
          </div>

          <!-- Desktop Menu -->
          <div class="hidden md:flex items-center space-x-8">
            <%= link_to "양식", templates_path, class: "text-gray-600 hover:text-navy-600 font-medium transition-colors" %>
            <%= link_to "가이드", guides_path, class: "text-gray-600 hover:text-navy-600 font-medium transition-colors" %>
            <%= link_to "도구", tools_path, class: "text-gray-600 hover:text-navy-600 font-medium transition-colors" %>
            <%= link_to "커뮤니티", "#community", class: "text-gray-600 hover:text-navy-600 font-medium transition-colors" %>
          </div>

          <!-- Auth Buttons -->
          <div class="hidden md:flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "text-gray-600 hover:text-navy-600 font-medium" %>
            <% else %>
              <%= link_to "로그인", new_user_session_path, class: "text-gray-600 hover:text-navy-600 font-medium" %>
              <%= link_to "회원가입", new_user_registration_path, class: "bg-navy-600 text-white px-4 py-2 rounded-lg hover:bg-navy-700 transition-colors font-medium" %>
            <% end %>
          </div>

          <!-- Mobile menu button -->
          <div class="md:hidden flex items-center">
            <button type="button" data-controller="mobile-menu" data-action="click->mobile-menu#toggle" class="text-gray-600 hover:text-navy-600">
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div class="md:hidden hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1 bg-white border-t">
          <%= link_to "양식", templates_path, class: "block px-3 py-2 text-gray-600 hover:bg-gray-100 rounded-md" %>
          <%= link_to "가이드", guides_path, class: "block px-3 py-2 text-gray-600 hover:bg-gray-100 rounded-md" %>
          <%= link_to "도구", tools_path, class: "block px-3 py-2 text-gray-600 hover:bg-gray-100 rounded-md" %>
          <%= link_to "커뮤니티", "#community", class: "block px-3 py-2 text-gray-600 hover:bg-gray-100 rounded-md" %>
          <div class="border-t pt-2 mt-2">
            <% if user_signed_in? %>
              <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "block w-full text-left px-3 py-2 text-gray-600 hover:bg-gray-100 rounded-md" %>
            <% else %>
              <%= link_to "로그인", new_user_session_path, class: "block px-3 py-2 text-gray-600 hover:bg-gray-100 rounded-md" %>
              <%= link_to "회원가입", new_user_registration_path, class: "block px-3 py-2 text-navy-600 font-medium hover:bg-gray-100 rounded-md" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-forest-50 border-l-4 border-forest-500 p-4">
        <div class="max-w-7xl mx-auto flex items-center">
          <svg class="h-5 w-5 text-forest-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <p class="text-forest-700"><%= notice %></p>
        </div>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-50 border-l-4 border-red-500 p-4">
        <div class="max-w-7xl mx-auto flex items-center">
          <svg class="h-5 w-5 text-red-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
          </svg>
          <p class="text-red-700"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-navy-800 text-white mt-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <!-- Brand -->
          <div class="col-span-1 md:col-span-2">
            <div class="flex items-center space-x-2 mb-4">
              <div class="w-8 h-8 bg-white rounded-lg flex items-center justify-center">
                <span class="text-navy-600 font-bold text-lg">실</span>
              </div>
              <span class="text-xl font-bold">실무</span>
            </div>
            <p class="text-navy-200 mb-4">감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원</p>
            <p class="text-navy-300 text-sm">공무원의 업무 효율과 삶의 질 향상을 위한 플랫폼</p>
          </div>

          <!-- Links -->
          <div>
            <h3 class="font-semibold mb-4">서비스</h3>
            <ul class="space-y-2 text-navy-200">
              <li><%= link_to "양식", templates_path, class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "가이드", guides_path, class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "도구", tools_path, class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "커뮤니티", "#community", class: "hover:text-white transition-colors" %></li>
            </ul>
          </div>

          <!-- Contact -->
          <div>
            <h3 class="font-semibold mb-4">문의</h3>
            <ul class="space-y-2 text-navy-200">
              <li><a href="mailto:hello@silmu.kr" class="hover:text-white transition-colors">hello@silmu.kr</a></li>
            </ul>
          </div>
        </div>

        <div class="border-t border-navy-700 mt-8 pt-8 text-center text-navy-300 text-sm">
          <p>&copy; <%= Time.current.year %> 실무 (silmu.kr). All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
