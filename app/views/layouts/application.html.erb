<!DOCTYPE html>
<html lang="ko" class="scroll-smooth">
  <head>
    <title><%= content_for(:title) || "실무 - 감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="실무">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="공무원을 위한 실무 가이드 - 감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원이 되세요.">
    <meta name="theme-color" content="#102a43">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Fonts %>
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/toss/tossface/dist/tossface.css">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">

    <%# Icons %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <style>
      .material-symbols-outlined {
        font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
      }
    </style>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen flex flex-col">
    <!-- Navigation — enhanced glass effect -->
    <nav class="bg-white/85 backdrop-blur-2xl border-b border-gray-100/60 sticky top-0 z-50 transition-all duration-300" id="main-nav">
      <div class="container-wide">
        <div class="flex justify-between h-16 lg:h-[4.5rem]">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "group flex items-center gap-3 transition-all duration-200 hover:scale-[1.02]" do %>
              <div class="w-9 h-9 bg-gradient-to-br from-navy-700 to-navy-900 rounded-xl flex items-center justify-center shadow-md group-hover:shadow-lg group-hover:shadow-navy-500/20 transition-all">
                <span class="text-white font-bold text-lg">실</span>
              </div>
              <span class="text-xl font-bold text-navy-900 tracking-tight">실무</span>
            <% end %>
          </div>

          <!-- Desktop Menu -->
          <div class="hidden lg:flex items-center gap-8">
            <%= link_to "도구", tools_path, class: "nav-link py-2" %>
            <%= link_to "법령가이드", guides_path, class: "nav-link py-2" %>
            <%= link_to "양식", templates_path, class: "nav-link py-2" %>
            <%= link_to "자료실", guide_resources_path, class: "nav-link py-2" %>
          </div>

          <!-- Auth Buttons -->
          <div class="hidden lg:flex items-center gap-4">
            <% if user_signed_in? %>
              <%= link_to mypage_path, class: "flex items-center gap-2 text-navy-600 hover:text-navy-900 font-medium transition-colors" do %>
                <span class="material-symbols-outlined text-xl">account_circle</span>
                마이페이지
              <% end %>
              <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "btn btn-ghost btn-sm" %>
            <% else %>
              <%= link_to "로그인", new_user_session_path, class: "text-navy-600 hover:text-navy-900 font-medium transition-colors" %>
              <%= link_to "시작하기", new_user_registration_path, class: "btn btn-primary btn-sm" %>
            <% end %>
          </div>

          <!-- Mobile menu button -->
          <div class="lg:hidden flex items-center">
            <button type="button" id="mobile-menu-btn" class="p-2 text-navy-600 hover:text-navy-900 hover:bg-gray-100 rounded-xl transition-colors">
              <span class="material-symbols-outlined text-2xl">menu</span>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div class="lg:hidden hidden" id="mobile-menu">
        <div class="px-4 py-4 space-y-1 bg-white border-t border-gray-100">
          <%= link_to "도구", tools_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <%= link_to "법령가이드", guides_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <%= link_to "양식", templates_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <%= link_to "자료실", guide_resources_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>

          <div class="h-px bg-gray-200 my-3"></div>

          <% if user_signed_in? %>
            <%= link_to mypage_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" do %>
              <span class="flex items-center gap-2">
                <span class="material-symbols-outlined">account_circle</span>
                마이페이지
              </span>
            <% end %>
            <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <% else %>
            <%= link_to "로그인", new_user_session_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
            <%= link_to "시작하기", new_user_registration_path, class: "block px-4 py-3 bg-navy-800 text-white text-center rounded-xl font-semibold hover:bg-navy-900 transition-colors" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-forest-50 border-l-4 border-forest-500 px-4 py-4 animate-slide-up">
        <div class="container-wide flex items-center gap-3">
          <span class="material-symbols-outlined text-forest-600">check_circle</span>
          <p class="text-forest-800 font-medium"><%= notice %></p>
        </div>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-50 border-l-4 border-red-500 px-4 py-4 animate-slide-up">
        <div class="container-wide flex items-center gap-3">
          <span class="material-symbols-outlined text-red-600">error</span>
          <p class="text-red-800 font-medium"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="flex-1">
      <%= yield %>
    </main>

    <!-- 퀵메뉴 (플로팅 버튼) — 강화 버전 -->
    <div class="fixed bottom-6 right-6 z-50" id="quick-menu">
      <!-- 펼쳐진 메뉴 — glass panel style -->
      <div class="hidden mb-3 space-y-1.5" id="quick-menu-items">
        <%= link_to contract_method_path, class: "flex items-center gap-3 px-4 py-3 bg-white/95 backdrop-blur-lg rounded-xl shadow-medium border border-gray-100/50 hover:shadow-strong hover:-translate-x-1 transition-all group", title: "계약방식 결정" do %>
          <span class="w-10 h-10 bg-forest-100 rounded-lg flex items-center justify-center group-hover:bg-forest-500 transition-all duration-200">
            <span class="material-symbols-outlined text-forest-600 group-hover:text-white transition-colors">gavel</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">계약방식 결정</span>
        <% end %>
        <%= link_to contract_documents_path, class: "flex items-center gap-3 px-4 py-3 bg-white/95 backdrop-blur-lg rounded-xl shadow-medium border border-gray-100/50 hover:shadow-strong hover:-translate-x-1 transition-all group", title: "서류 생성기" do %>
          <span class="w-10 h-10 bg-navy-100 rounded-lg flex items-center justify-center group-hover:bg-navy-600 transition-all duration-200">
            <span class="material-symbols-outlined text-navy-600 group-hover:text-white transition-colors">fact_check</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">서류 생성기</span>
        <% end %>
        <a href="/forms/견적서검토.html" class="flex items-center gap-3 px-4 py-3 bg-white/95 backdrop-blur-lg rounded-xl shadow-medium border border-gray-100/50 hover:shadow-strong hover:-translate-x-1 transition-all group" title="견적서 검토">
          <span class="w-10 h-10 bg-point-100 rounded-lg flex items-center justify-center group-hover:bg-point-500 transition-all duration-200">
            <span class="material-symbols-outlined text-point-600 group-hover:text-white transition-colors">fact_check</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">견적서 검토</span>
        </a>
        <%= link_to estimated_price_path, class: "flex items-center gap-3 px-4 py-3 bg-white/95 backdrop-blur-lg rounded-xl shadow-medium border border-gray-100/50 hover:shadow-strong hover:-translate-x-1 transition-all group", title: "예정가격 계산기" do %>
          <span class="w-10 h-10 bg-forest-100 rounded-lg flex items-center justify-center group-hover:bg-forest-500 transition-all duration-200">
            <span class="material-symbols-outlined text-forest-600 group-hover:text-white transition-colors">calculate</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">예정가격 계산기</span>
        <% end %>
        <%= link_to travel_calculator_path, class: "flex items-center gap-3 px-4 py-3 bg-white/95 backdrop-blur-lg rounded-xl shadow-medium border border-gray-100/50 hover:shadow-strong hover:-translate-x-1 transition-all group", title: "여비계산기" do %>
          <span class="w-10 h-10 bg-warm-100 rounded-lg flex items-center justify-center group-hover:bg-warm-500 transition-all duration-200">
            <span class="material-symbols-outlined text-warm-600 group-hover:text-white transition-colors">directions_car</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">여비계산기</span>
        <% end %>
        <%= link_to tools_path, class: "flex items-center gap-3 px-4 py-3 bg-forest-50 backdrop-blur-lg rounded-xl shadow-medium border border-forest-200/50 hover:shadow-strong hover:-translate-x-1 transition-all group", title: "전체 도구" do %>
          <span class="w-10 h-10 bg-forest-500 rounded-lg flex items-center justify-center">
            <span class="material-symbols-outlined text-white">apps</span>
          </span>
          <span class="text-sm font-bold text-forest-700">전체 14개 도구 보기</span>
        <% end %>
      </div>

      <!-- 메인 버튼 + 라벨 -->
      <div class="flex items-center gap-3 justify-end">
        <div id="quick-menu-label" class="hidden px-4 py-2.5 bg-navy-800 text-white text-sm font-medium rounded-xl shadow-lg shadow-navy-900/30 whitespace-nowrap animate-fade-in relative">
          실무 도구 모음
          <div class="absolute -right-1.5 top-1/2 -translate-y-1/2 w-3 h-3 bg-navy-800 rotate-45"></div>
        </div>
        <button id="quick-menu-btn" class="w-16 h-16 bg-gradient-to-br from-navy-700 to-navy-900 text-white rounded-2xl shadow-lg shadow-navy-900/30 hover:shadow-xl hover:shadow-navy-900/40 hover:scale-105 active:scale-95 transition-all duration-200 flex flex-col items-center justify-center group relative">
          <span class="material-symbols-outlined text-2xl transition-transform group-hover:rotate-12" id="quick-menu-icon">widgets</span>
          <span class="text-[9px] font-semibold mt-0.5 opacity-80" id="quick-menu-text">도구</span>
          <!-- 알림 배지 -->
          <span class="absolute -top-1 -right-1 w-5 h-5 bg-forest-500 rounded-full flex items-center justify-center text-[10px] font-bold text-white shadow-sm" id="quick-menu-badge">14</span>
        </button>
      </div>
    </div>

    <style>
      #quick-menu-items {
        animation: slideUp 0.2s ease;
      }
      @keyframes slideUp {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
      }
      #quick-menu-items.hidden {
        display: none !important;
      }
      @keyframes pulseOnce {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
      }
      .quick-menu-pulse {
        animation: pulseOnce 0.5s ease 2;
      }
    </style>

    <!-- Footer — enhanced with gradient border -->
    <footer class="bg-navy-900 text-white mt-auto relative">
      <!-- Top gradient line -->
      <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-point-500/40 to-transparent"></div>
      <div class="container-wide py-16 lg:py-20">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 lg:gap-8">
          <!-- Brand -->
          <div class="lg:col-span-2">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 bg-white rounded-xl flex items-center justify-center shadow-md">
                <span class="text-navy-900 font-bold text-xl">실</span>
              </div>
              <span class="text-2xl font-bold">실무</span>
            </div>
            <p class="text-navy-200 text-lg mb-2">감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원</p>
            <p class="text-navy-400 text-sm">공무원의 업무 효율과 삶의 질 향상을 위한 플랫폼</p>

            <!-- Social Links -->
            <div class="flex gap-3 mt-6">
              <a href="mailto:hello@silmu.kr" class="w-10 h-10 bg-navy-800 hover:bg-navy-700 rounded-xl flex items-center justify-center transition-colors" title="이메일 문의">
                <span class="material-symbols-outlined text-navy-300">mail</span>
              </a>
              <%= link_to guide_resources_path, class: "w-10 h-10 bg-navy-800 hover:bg-navy-700 rounded-xl flex items-center justify-center transition-colors", title: "자료실" do %>
                <span class="material-symbols-outlined text-navy-300">library_books</span>
              <% end %>
            </div>
          </div>

          <!-- Links -->
          <div>
            <h3 class="font-semibold text-lg mb-5">서비스</h3>
            <ul class="space-y-3">
              <li><%= link_to "도구", tools_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "법령가이드", guides_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "양식", templates_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "자료실", guide_resources_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "자주 묻는 질문", faq_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
            </ul>
          </div>

          <!-- Contact -->
          <div>
            <h3 class="font-semibold text-lg mb-5">문의</h3>
            <ul class="space-y-3">
              <li>
                <a href="mailto:hello@silmu.kr" class="text-navy-300 hover:text-white transition-colors flex items-center gap-2">
                  <span class="material-symbols-outlined text-lg">mail</span>
                  hello@silmu.kr
                </a>
              </li>
              <li class="text-navy-400 text-sm pt-2">
                평일 09:00 - 18:00<br>
                (주말/공휴일 휴무)
              </li>
            </ul>
          </div>
        </div>

        <!-- Bottom -->
        <div class="border-t border-navy-800 mt-12 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
          <p class="text-navy-400 text-sm">&copy; <%= Time.current.year %> 실무 (silmu.kr). All rights reserved.</p>
          <div class="flex gap-6 text-sm">
            <a href="javascript:void(0)" onclick="alert('이용약관 페이지는 준비 중입니다.')" class="text-navy-400 hover:text-white transition-colors cursor-pointer">이용약관</a>
            <a href="javascript:void(0)" onclick="alert('개인정보처리방침 페이지는 준비 중입니다.')" class="text-navy-400 hover:text-white transition-colors cursor-pointer">개인정보처리방침</a>
          </div>
        </div>
      </div>
    </footer>

    <script>
      (function() {
        // Mobile Menu Toggle
        const mobileMenuBtn = document.getElementById('mobile-menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');

        if (mobileMenuBtn && mobileMenu) {
          mobileMenuBtn.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
            const icon = mobileMenuBtn.querySelector('.material-symbols-outlined');
            icon.textContent = mobileMenu.classList.contains('hidden') ? 'menu' : 'close';
          });
        }

        // Navbar scroll effect
        window.addEventListener('scroll', () => {
          const nav = document.getElementById('main-nav');
          if (!nav) return;
          if (window.scrollY > 10) {
            nav.classList.add('shadow-soft');
            nav.style.borderBottomColor = 'rgba(0,0,0,0.06)';
          } else {
            nav.classList.remove('shadow-soft');
            nav.style.borderBottomColor = '';
          }
        });

        // Quick Menu Toggle
        const quickMenuBtn = document.getElementById('quick-menu-btn');
        const quickMenuItems = document.getElementById('quick-menu-items');
        const quickMenuIcon = document.getElementById('quick-menu-icon');
        const quickMenuText = document.getElementById('quick-menu-text');
        const quickMenuLabel = document.getElementById('quick-menu-label');
        const quickMenuBadge = document.getElementById('quick-menu-badge');

        if (quickMenuBtn && quickMenuItems) {
          quickMenuBtn.addEventListener('click', () => {
            quickMenuItems.classList.toggle('hidden');
            if (quickMenuLabel) quickMenuLabel.classList.add('hidden');
            if (quickMenuItems.classList.contains('hidden')) {
              quickMenuIcon.textContent = 'widgets';
              if (quickMenuText) quickMenuText.textContent = '도구';
              if (quickMenuBadge) quickMenuBadge.classList.remove('hidden');
            } else {
              quickMenuIcon.textContent = 'close';
              if (quickMenuText) quickMenuText.textContent = '닫기';
              if (quickMenuBadge) quickMenuBadge.classList.add('hidden');
              localStorage.setItem('silmu_quick_menu_seen', 'true');
            }
          });

          // 외부 클릭 시 닫기
          document.addEventListener('click', (e) => {
            if (!quickMenuBtn.contains(e.target) && !quickMenuItems.contains(e.target)) {
              quickMenuItems.classList.add('hidden');
              quickMenuIcon.textContent = 'widgets';
              if (quickMenuText) quickMenuText.textContent = '도구';
              if (quickMenuBadge) quickMenuBadge.classList.remove('hidden');
            }
          });

          // 첫 방문 시 라벨 표시 + 버튼 펄스 효과
          if (!localStorage.getItem('silmu_quick_menu_seen')) {
            setTimeout(() => {
              if (quickMenuLabel) quickMenuLabel.classList.remove('hidden');
              quickMenuBtn.classList.add('quick-menu-pulse');
              setTimeout(() => {
                if (quickMenuLabel) quickMenuLabel.classList.add('hidden');
                quickMenuBtn.classList.remove('quick-menu-pulse');
              }, 5000);
            }, 2000);
          }
        }
      })();
    </script>
  </body>
</html>
