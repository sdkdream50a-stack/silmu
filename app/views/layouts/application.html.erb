<!DOCTYPE html>
<html lang="ko" class="scroll-smooth">
  <head>
    <title><%= content_for(:title) || "실무 - 감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="실무">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="공무원을 위한 실무 가이드 - 감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원이 되세요.">
    <meta name="theme-color" content="#102a43">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Fonts %>
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/toss/tossface/dist/tossface.css">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">

    <%# Icons %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <style>
      .material-symbols-outlined {
        font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
      }
    </style>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen flex flex-col">
    <!-- Navigation -->
    <nav class="bg-white/80 backdrop-blur-xl border-b border-gray-100 sticky top-0 z-50 transition-all duration-300">
      <div class="container-wide">
        <div class="flex justify-between h-16 lg:h-18">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "group flex items-center gap-3 transition-transform duration-200 hover:scale-[1.02]" do %>
              <div class="w-9 h-9 bg-gradient-to-br from-navy-700 to-navy-900 rounded-xl flex items-center justify-center shadow-md group-hover:shadow-lg transition-shadow">
                <span class="text-white font-bold text-lg">실</span>
              </div>
              <span class="text-xl font-bold text-navy-900 tracking-tight">실무</span>
            <% end %>
          </div>

          <!-- Desktop Menu -->
          <div class="hidden lg:flex items-center gap-8">
            <%= link_to "가이드", guides_path, class: "nav-link py-2" %>
            <%= link_to "도구", tools_path, class: "nav-link py-2" %>
            <%= link_to "양식", templates_path, class: "nav-link py-2" %>
            <%= link_to "커뮤니티", community_path, class: "nav-link py-2" %>
          </div>

          <!-- Auth Buttons -->
          <div class="hidden lg:flex items-center gap-4">
            <% if user_signed_in? %>
              <%= link_to mypage_path, class: "flex items-center gap-2 text-navy-600 hover:text-navy-900 font-medium transition-colors" do %>
                <span class="material-symbols-outlined text-xl">account_circle</span>
                마이페이지
              <% end %>
              <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "btn btn-ghost btn-sm" %>
            <% else %>
              <%= link_to "로그인", new_user_session_path, class: "text-navy-600 hover:text-navy-900 font-medium transition-colors" %>
              <%= link_to "시작하기", new_user_registration_path, class: "btn btn-primary btn-sm" %>
            <% end %>
          </div>

          <!-- Mobile menu button -->
          <div class="lg:hidden flex items-center">
            <button type="button" id="mobile-menu-btn" class="p-2 text-navy-600 hover:text-navy-900 hover:bg-gray-100 rounded-xl transition-colors">
              <span class="material-symbols-outlined text-2xl">menu</span>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div class="lg:hidden hidden" id="mobile-menu">
        <div class="px-4 py-4 space-y-1 bg-white border-t border-gray-100">
          <%= link_to "가이드", guides_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <%= link_to "도구", tools_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <%= link_to "양식", templates_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <%= link_to "커뮤니티", community_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>

          <div class="h-px bg-gray-200 my-3"></div>

          <% if user_signed_in? %>
            <%= link_to mypage_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" do %>
              <span class="flex items-center gap-2">
                <span class="material-symbols-outlined">account_circle</span>
                마이페이지
              </span>
            <% end %>
            <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
          <% else %>
            <%= link_to "로그인", new_user_session_path, class: "block px-4 py-3 text-navy-700 hover:bg-gray-50 rounded-xl font-medium transition-colors" %>
            <%= link_to "시작하기", new_user_registration_path, class: "block px-4 py-3 bg-navy-800 text-white text-center rounded-xl font-semibold hover:bg-navy-900 transition-colors" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-forest-50 border-l-4 border-forest-500 px-4 py-4 animate-slide-up">
        <div class="container-wide flex items-center gap-3">
          <span class="material-symbols-outlined text-forest-600">check_circle</span>
          <p class="text-forest-800 font-medium"><%= notice %></p>
        </div>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-50 border-l-4 border-red-500 px-4 py-4 animate-slide-up">
        <div class="container-wide flex items-center gap-3">
          <span class="material-symbols-outlined text-red-600">error</span>
          <p class="text-red-800 font-medium"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="flex-1">
      <%= yield %>
    </main>

    <!-- 퀵메뉴 (플로팅 버튼) -->
    <div class="fixed bottom-6 right-6 z-50" id="quick-menu">
      <!-- 펼쳐진 메뉴 -->
      <div class="hidden mb-3 space-y-2" id="quick-menu-items">
        <%= link_to contract_method_path, class: "flex items-center gap-3 px-4 py-3 bg-white rounded-xl shadow-lg border border-gray-100 hover:shadow-xl transition-all group", title: "계약방식 결정" do %>
          <span class="w-10 h-10 bg-forest-100 rounded-lg flex items-center justify-center group-hover:bg-forest-200 transition-colors">
            <span class="material-symbols-outlined text-forest-600">gavel</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">계약방식 결정</span>
        <% end %>
        <%= link_to contract_documents_path, class: "flex items-center gap-3 px-4 py-3 bg-white rounded-xl shadow-lg border border-gray-100 hover:shadow-xl transition-all group", title: "서류 체크리스트" do %>
          <span class="w-10 h-10 bg-point-100 rounded-lg flex items-center justify-center group-hover:bg-point-200 transition-colors">
            <span class="material-symbols-outlined text-point-600">checklist</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">서류 체크리스트</span>
        <% end %>
        <%= link_to pre_contract_checklist_path, class: "flex items-center gap-3 px-4 py-3 bg-white rounded-xl shadow-lg border border-gray-100 hover:shadow-xl transition-all group", title: "사전 체크리스트" do %>
          <span class="w-10 h-10 bg-navy-100 rounded-lg flex items-center justify-center group-hover:bg-navy-200 transition-colors">
            <span class="material-symbols-outlined text-navy-600">checklist_rtl</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">사전 체크리스트</span>
        <% end %>
        <%= link_to contract_flow_path, class: "flex items-center gap-3 px-4 py-3 bg-white rounded-xl shadow-lg border border-gray-100 hover:shadow-xl transition-all group", title: "계약 흐름도" do %>
          <span class="w-10 h-10 bg-warm-100 rounded-lg flex items-center justify-center group-hover:bg-warm-200 transition-colors">
            <span class="material-symbols-outlined text-warm-600">account_tree</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">계약 흐름도</span>
        <% end %>
        <a href="https://www.g2b.go.kr" target="_blank" class="flex items-center gap-3 px-4 py-3 bg-white rounded-xl shadow-lg border border-gray-100 hover:shadow-xl transition-all group" title="나라장터">
          <span class="w-10 h-10 bg-surface-100 rounded-lg flex items-center justify-center group-hover:bg-surface-200 transition-colors">
            <span class="material-symbols-outlined text-navy-500">open_in_new</span>
          </span>
          <span class="text-sm font-semibold text-navy-800">나라장터</span>
        </a>
      </div>

      <!-- 메인 버튼 -->
      <button id="quick-menu-btn" class="w-14 h-14 bg-gradient-to-br from-navy-700 to-navy-900 text-white rounded-full shadow-lg hover:shadow-xl transition-all flex items-center justify-center group">
        <span class="material-symbols-outlined text-2xl transition-transform group-hover:rotate-90" id="quick-menu-icon">widgets</span>
      </button>
    </div>

    <style>
      #quick-menu-items {
        animation: slideUp 0.2s ease;
      }
      @keyframes slideUp {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
      }
      #quick-menu-items.hidden {
        display: none !important;
      }
    </style>

    <!-- Footer -->
    <footer class="bg-navy-900 text-white mt-auto">
      <div class="container-wide py-16 lg:py-20">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 lg:gap-8">
          <!-- Brand -->
          <div class="lg:col-span-2">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 bg-white rounded-xl flex items-center justify-center">
                <span class="text-navy-900 font-bold text-xl">실</span>
              </div>
              <span class="text-2xl font-bold">실무</span>
            </div>
            <p class="text-navy-200 text-lg mb-2">감사 걱정 없는, 퇴근 있는, 은퇴 준비되는 공무원</p>
            <p class="text-navy-400">공무원의 업무 효율과 삶의 질 향상을 위한 플랫폼</p>

            <!-- Social Links -->
            <div class="flex gap-3 mt-6">
              <a href="#" class="w-10 h-10 bg-navy-800 hover:bg-navy-700 rounded-xl flex items-center justify-center transition-colors">
                <span class="material-symbols-outlined text-navy-300">mail</span>
              </a>
              <a href="#" class="w-10 h-10 bg-navy-800 hover:bg-navy-700 rounded-xl flex items-center justify-center transition-colors">
                <span class="material-symbols-outlined text-navy-300">forum</span>
              </a>
            </div>
          </div>

          <!-- Links -->
          <div>
            <h3 class="font-semibold text-lg mb-5">서비스</h3>
            <ul class="space-y-3">
              <li><%= link_to "가이드", guides_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "도구", tools_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "양식", templates_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
              <li><%= link_to "커뮤니티", community_path, class: "text-navy-300 hover:text-white transition-colors" %></li>
            </ul>
          </div>

          <!-- Contact -->
          <div>
            <h3 class="font-semibold text-lg mb-5">문의</h3>
            <ul class="space-y-3">
              <li>
                <a href="mailto:hello@silmu.kr" class="text-navy-300 hover:text-white transition-colors flex items-center gap-2">
                  <span class="material-symbols-outlined text-lg">mail</span>
                  hello@silmu.kr
                </a>
              </li>
              <li class="text-navy-400 text-sm pt-2">
                평일 09:00 - 18:00<br>
                (주말/공휴일 휴무)
              </li>
            </ul>
          </div>
        </div>

        <!-- Bottom -->
        <div class="border-t border-navy-800 mt-12 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
          <p class="text-navy-400 text-sm">&copy; <%= Time.current.year %> 실무 (silmu.kr). All rights reserved.</p>
          <div class="flex gap-6 text-sm">
            <a href="#" class="text-navy-400 hover:text-white transition-colors">이용약관</a>
            <a href="#" class="text-navy-400 hover:text-white transition-colors">개인정보처리방침</a>
          </div>
        </div>
      </div>
    </footer>

    <script>
      // Mobile Menu Toggle
      const mobileMenuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');

      if (mobileMenuBtn && mobileMenu) {
        mobileMenuBtn.addEventListener('click', () => {
          mobileMenu.classList.toggle('hidden');
          const icon = mobileMenuBtn.querySelector('.material-symbols-outlined');
          icon.textContent = mobileMenu.classList.contains('hidden') ? 'menu' : 'close';
        });
      }

      // Navbar scroll effect
      window.addEventListener('scroll', () => {
        const nav = document.querySelector('nav');
        if (window.scrollY > 10) {
          nav.classList.add('shadow-soft');
        } else {
          nav.classList.remove('shadow-soft');
        }
      });

      // Quick Menu Toggle
      const quickMenuBtn = document.getElementById('quick-menu-btn');
      const quickMenuItems = document.getElementById('quick-menu-items');
      const quickMenuIcon = document.getElementById('quick-menu-icon');

      if (quickMenuBtn && quickMenuItems) {
        quickMenuBtn.addEventListener('click', () => {
          quickMenuItems.classList.toggle('hidden');
          if (quickMenuItems.classList.contains('hidden')) {
            quickMenuIcon.textContent = 'widgets';
            quickMenuIcon.style.transform = 'rotate(0deg)';
          } else {
            quickMenuIcon.textContent = 'close';
            quickMenuIcon.style.transform = 'rotate(0deg)';
          }
        });

        // 외부 클릭 시 닫기
        document.addEventListener('click', (e) => {
          if (!quickMenuBtn.contains(e.target) && !quickMenuItems.contains(e.target)) {
            quickMenuItems.classList.add('hidden');
            quickMenuIcon.textContent = 'widgets';
          }
        });
      }
    </script>
  </body>
</html>
