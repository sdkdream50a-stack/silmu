<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 30px; text-align: center; border-radius: 10px 10px 0 0; }
    .content { background: #fff; padding: 30px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 10px 10px; }
    .section { margin-bottom: 30px; }
    .section h2 { color: #667eea; border-bottom: 2px solid #667eea; padding-bottom: 10px; margin-bottom: 15px; }
    .stat-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; }
    .stat-box { background: #f9fafb; padding: 15px; border-radius: 8px; text-align: center; }
    .stat-number { font-size: 32px; font-weight: bold; color: #667eea; }
    .stat-label { color: #6b7280; font-size: 14px; margin-top: 5px; }
    .list { list-style: none; padding: 0; }
    .list li { padding: 10px; background: #f9fafb; margin-bottom: 8px; border-radius: 5px; }
    .warning { background: #fef3c7; border-left: 4px solid #f59e0b; padding: 15px; border-radius: 5px; }
    .footer { text-align: center; margin-top: 30px; padding-top: 20px; border-top: 1px solid #e5e7eb; color: #6b7280; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0;">📊 SEO 주간 리포트</h1>
      <p style="margin: 10px 0 0 0; opacity: 0.9;"><%= @date.strftime('%Y년 %m월 %d일') %></p>
    </div>

    <div class="content">
      <!-- 콘텐츠 현황 -->
      <div class="section">
        <h2>📚 콘텐츠 현황</h2>
        <div class="stat-grid">
          <div class="stat-box">
            <div class="stat-number"><%= @report[:content][:topics_count] %></div>
            <div class="stat-label">토픽</div>
          </div>
          <div class="stat-box">
            <div class="stat-number"><%= @report[:content][:audit_cases_count] %></div>
            <div class="stat-label">감사사례</div>
          </div>
          <div class="stat-box">
            <div class="stat-number"><%= @report[:content][:tools_count] %></div>
            <div class="stat-label">도구</div>
          </div>
          <div class="stat-box">
            <div class="stat-number"><%= @report[:content][:topics_with_faq] %></div>
            <div class="stat-label">FAQ 포함 토픽</div>
          </div>
        </div>
      </div>

      <!-- 트래픽 Top 5 -->
      <div class="section">
        <h2>📈 인기 토픽 (최근 7일)</h2>
        <ol class="list">
          <% @report[:traffic][:top_topics].each do |topic| %>
            <li><strong><%= topic[:name] %></strong> - <%= topic[:views] %>회 조회</li>
          <% end %>
        </ol>
      </div>

      <!-- SEO 상태 -->
      <div class="section">
        <h2>🔍 SEO 상태</h2>
        <ul class="list">
          <li><strong>Sitemap 페이지 수:</strong> <%= @report[:seo][:sitemap_urls] %>개</li>
          <li><strong>평균 메타 설명 길이:</strong> <%= @report[:seo][:avg_meta_length] %>자</li>
          <li><strong>구조화 데이터:</strong> <%= @report[:seo][:topics_with_structured_data] %>개 토픽</li>
          <li><strong>Canonical 문제:</strong> <%= @report[:seo][:canonical_issues] %>개</li>
        </ul>
      </div>

      <!-- 주의사항 -->
      <div class="section">
        <h2>⚠️ 주의사항</h2>
        <% @report[:warnings].each do |warning| %>
          <div class="warning" style="margin-bottom: 10px;">
            <%= warning %>
          </div>
        <% end %>
      </div>

      <div class="footer">
        <p>실무.kr SEO 자동 모니터링 시스템</p>
        <p><a href="https://silmu.kr" style="color: #667eea; text-decoration: none;">실무.kr 방문하기</a></p>
      </div>
    </div>
  </div>
</body>
</html>
